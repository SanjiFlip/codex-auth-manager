# 安全说明

当前维护版本为 **0.6.2 预览版**。请使用 [最新发布的修复版本](https://github.com/SanjiFlip/codex-auth-manager/releases/tag/v0.6.2)；旧版本不单独维护安全补丁。项目尚未完成独立安全审计。

## 报告漏洞

请勿在公开 Issue 中粘贴真实账号信息、令牌、auth.json、账户库或未脱敏日志。请优先使用本仓库 Security 页的私密漏洞报告入口；若入口不可用，可先提交不含敏感细节的联系请求，由维护者确认私密沟通方式。

## 凭据与数据边界

- 本工具调用本机官方 Codex CLI 完成授权；请在官方授权页面核对身份。
- 账户库在 Windows 使用 DPAPI、macOS 使用 Keychain 加密，依赖当前系统用户；索引中的邮箱、名称等元数据并非全部加密。
- Codex 文件模式的 auth.json 含敏感令牌。任何有权读取这些文件的进程仍可能访问凭据。
- 导入导出的加密文件也属于敏感数据，不能上传到仓库。
- Pro 五小时设置仅影响展示，不改变官方限制或权益。
- Windows 安装包未签名；macOS 仅 ad-hoc 签名，未完成 Developer ID 签名与公证；断电或并发修改文件不具备完整事务恢复保证。

提交前检查暂存区内容；忽略规则不能替代审查，也不能清除已上传的历史。若令牌已泄露，应先撤销或轮换，再处理 Git 历史。


## 会话蒸馏与 Skills 安装

蒸馏需要把用户明确选中的消息发送给本机当前登录 Codex CLI 使用的官方模型，消耗相应额度。常见令牌遮盖不等于完整脱敏。知识库通过系统加密保护，Markdown 导出为明文；知识库不写入 Codex 原生记忆目录。详见 [数据与运行边界](docs/KNOWLEDGE.md)。

Skills 商店安装来自用户选择的公开或有权限访问的私有仓库，不自动执行脚本；技能运行时可能使用工具或访问数据，请安装前审阅原文、来源和许可证。断点检查点与知识库一样通过系统加密保存，导出文件仍为明文。

可选 GitHub Token 通过系统加密存于应用数据目录，仅发往 GitHub API，不提交源码或导出。建议使用仅满足所需仓库 Contents 只读权限的专用 Token；不需要写入权限。

可选 Skills 翻译默认关闭，仅在用户点击翻译时向勾选的第三方公共服务发送所选文本；本地和私有技能同样适用。不会附带账号凭据，文本自身包含的敏感信息仍可能外发。译文仅存于进程内存，不改写原始技能文件；服务列表、额度与网络限制见 [Skills 文档](docs/SKILLS.md#可选中文翻译)。
