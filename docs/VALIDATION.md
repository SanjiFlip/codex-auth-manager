# 验证记录

## 当前发布：v0.4.1

- [Release 与校验文件](https://github.com/SanjiFlip/codex-auth-manager/releases/tag/v0.4.1)。发布源码：`f19bbaeb7f6390da4e9659dc56c5a59b0d49296a`。
- [GitHub Actions 35599898366](https://github.com/SanjiFlip/codex-auth-manager/actions/runs/35599898366)：Windows、macOS arm64 / x64 全部成功；Mac 包含原生窗口、隔离账号与实际打包应用检查。
- Windows 本地 40 项单元测试通过，1 项 Mac 原生检查按平台跳过；实时刷新、日志轮换与实际 EXE 隔离检查通过，51 个打包源码文件逐字节一致。
- EXE、两种 DMG、两种 ZIP 的上传大小和 SHA-256 已校验，公开下载链接可用。Mac 包未完成 Developer ID 签名与公证。
- 额度故障以合成事件复现；未捕获用户当时的原始异常事件，未执行真实账号在线切换。

以下按历史验证时间保留记录，各版本测试数量、行为和未验证项仅代表当时状态。

## 0.3.0 验证记录

验证环境：Windows，2026-09-12。所有账号写入测试均使用临时目录与合成凭据，没有切换或重启用户正在运行的 Codex。

| 验证项 | 结果与边界 |
| --- | --- |
| JavaScript 语法 | npm run check 通过 |
| 自动化测试 | npm test：17 / 17 通过；登录取消、并发拒绝、令牌不外泄、Pro 档位、额度窗口、统计口径、切换顺序、错误恢复 |
| 源码 Electron 集成 | scripts/smoke-electron.js 通过；真实 IPC、DPAPI、改名、删除、凭据替换、失败恢复；OS 进程适配使用替身；原生浮窗创建 / 加载 / 隐藏 / 置顶通过 |
| 浏览器 UI | scripts/ui-smoke.js 通过；添加与取消、搜索、模拟切换、隐私、统计、额度、诊断、活动记录、浮窗布局和按钮 |
| Windows 发现 | 识别到本机 Store Codex 的 AppID 与 ChatGPT.exe 路径；只读发现，没有停止真实进程 |
| 官方 CLI | 0.154.0；打包后的 Electron 可执行官方 JS CLI；空账户 app-server 返回预期未登录错误；退出与临时目录清理通过 |
| 打包 | electron-builder 生成 0.3.0 x64 NSIS 安装包与 win-unpacked；EXE 版本资源与紫色图标更新 |
| 包内真实启动 | scripts/packaged-smoke.js 启动构建的 EXE；通过真实 bridge 保存合成账号，验证独立库、Pro 5x、紫色样式、原生浮窗、置顶、未知额度和 auth 文件不被改写 |
| 包内 PowerShell | windows-codex.ps1 放入 app.asar.unpacked；修复不能直接执行 ASAR 内脚本的问题 |
| 报告过的闪退 | 隔离启动未复现。新增启动组件失败提示，不宣称已排除所有崩溃原因 |

## 仍未验证

- 真人完成浏览器授权、官方额度有登录状态时返回，以及网络 / 账号权限的实际结果。
- 实际运行中的 Codex 正常退出、切换后重启，并在 UI 中确认在线身份。
- 本次没有覆盖安装用户已安装的旧版本。使用新安装包更新才能看到新版。
- 打包产物未签名；electron-builder 的 signing 日志不代表拥有发行者证书。

## 检查中修复的问题

- PowerShell 帮助脚本原先在 ASAR 内，打包后无法作为外部脚本运行：改为 asarUnpack 并解析到外部路径。
- 官方子进程退出后临时文件短暂被锁：等待 close，清理采用有限重试。
- 官方套餐缓存可能覆盖凭据中已变化的套餐：身份套餐变化时清除旧官方快照。
- 原生浮窗创建后的首次 URL 为空：集成测试改为等待加载并设上限；并非将测试等待误报为程序崩溃。
- electron-builder 的旧版工具包含 macOS 符号链接，当前 Windows 不能解压：仅将下载包中的 Windows 工具解压到构建缓存，保留 EXE 图标与版本编辑。

## 截图

以下是本地验证产物路径，按忽略规则不提交到 GitHub；不代表仓库内提供这些图片。

- output/playwright/aurora-dashboard.png：演示主界面。
- output/playwright/aurora-statistics.png：演示统计页。
- output/playwright/aurora-meter.png：演示浮窗。
- output/playwright/packaged-main.png 与 packaged-meter.png：实际 EXE，合成账号隔离测试；没有演示额度填充。

## 0.3.1 增量验证（2026-09-12）

- npm test：18 / 18 通过，新增 Pro 5x / 20x 默认关闭、显式开启及其他套餐不受影响的测试。
- npm run check 通过；新版安装包和 EXE 0.3.1 已构建。
- 浏览器验证 Pro 开关、账号卡片、默认关闭时的浮窗、周额度仍显示；所有原有 UI 流程通过。
- 860 × 620 桌面小窗口下统计 / 额度 / 设置页面无横向溢出，检查明暗模式。
- 实际 EXE 在隔离凭据库启动通过；更新设置后，原生浮窗从「已关闭展示」变为「额度待获取」，再关闭；设置保存后读取为 true，当前 auth 内容未改变。
- 悬浮窗辅助文字改为 11–12px；主界面正文 14px，标签 12–13px；采用浅灰、白色和系统蓝配色，图标与原生标题栏同步。
- 真实账号授权、真实 Codex 切换验收边界与 0.3.0 相同；没有覆盖安装用户目录中的旧版。

## 0.3.2 悬浮窗验证（2026-09-12）

- npm run check 与 18 项单元测试通过。
- scripts/meter-smoke.js 验证：Pro 默认周额度主环且无五小时文本、无重复周卡；Plus 双窗口；刷新保留选择；取消不切换；确认后更新身份与额度；方向键 / Enter / Escape；明暗列表；360 × 560 下无内容溢出。
- 账号名称通过 textContent 写入，长名称省略并提供标题；列表设置最大高度与内部滚动。
- scripts/packaged-smoke.js 通过实际 0.3.2 EXE 验证：默认周主环、开启 Pro 五小时设置后切回双窗口，真实 IPC / DPAPI、原生浮窗和置顶可用。
- 未执行真实 Codex 重启；UI 切换验收使用演示账号，打包验证使用独立临时账号库。
- 截图：output/playwright/meter-weekly.png、meter-accounts.png、meter-plus.png、meter-dark-picker.png。

## 0.3.3 标记位置修复（2026-09-12）

- 主窗口从整页滚动改为 page-scroll 独立滚动；topbar 固定布局，原生桌面端预留 160px 窗口按钮区域。页面切换重置滚动，同页数据刷新不重置。
- scripts/chrome-smoke.js 验证 1340 × 920、860 × 620 的滚动状态：标题栏 top=0、滚动容器在标题栏之下、window.scrollY=0、右侧状态文字不进入原生控件区域。
- 悬浮窗 logo、顶部工具按钮和三个统计图标改为 SVG；统计图标底座 28 × 28、图形 18 × 18，检查中心对齐。
- 内部卡片与壳体改为不透明颜色，窗口外部仍保留透明圆角。原 CSS backdrop-filter 不会模糊 Windows 后方窗口文字，因此移除了依赖该效果的半透明壳体。
- 双额度模式增大图标后一度超出 4px，调小组件间距后 scripts/meter-smoke.js 无裁切检查通过。
- scripts/ui-smoke.js、scripts/meter-smoke.js、scripts/chrome-smoke.js 通过。
- 真实打包 EXE 0.3.3 通过隔离账户库启动检查，新增标题栏布局、原生标题栏颜色接口、三个 SVG 图标检查；保留 Pro 设置联动、加密与 IPC 检查。
- 构建新版安装包；没有覆盖安装用户目录里的旧版，也没有切换真实 Codex 账号。
- 截图：output/playwright/titlebar-scrolled.png、meter-icons.png；实际 EXE：packaged-main.png、packaged-meter.png。

## 0.3.4 自动刷新验证（2026-09-13）

- 使用合成账户启动真实 main / preload / 两个 renderer，先在旧版运行 scripts/realtime-smoke.js：新增 JSONL 用量后，两个窗口在 9 秒内没有更新，测试失败。
- 修复后同一复现通过：日志触发两窗口统计更新，手动官方刷新传给浮窗，随后更晚的本地额度覆盖旧官方快照；本地 Unix 秒恢复时间正常。
- 21 项单元测试通过，覆盖请求合并、自动补查间隔、失败退避、手动重试、额度窗口按观测时间选择；保留原有登录、切换回滚和用量口径测试。
- scripts/meter-smoke.js 验证待切换选择保留、取消 / 确认、键盘列表、明暗主题和布局无裁切。
- 自动官方补查使用合成服务替身验证；真实账号的服务端更新延迟仍未验收。发布 EXE 的隔离测试通过 PATH 中的合成 CLI 验证自动查询，不向真实服务发送测试凭据。

## 0.3.5 本地同步与隐藏查询验证（2026-09-13）

- 检查本机 Codex CLI 0.154.0 启动脚本：npm 包装层再 spawn 原生 CLI 时未设置 windowsHide；此前管理工具的隐藏参数只作用于包装层。
- 新解析器直接查找原生 EXE，去掉查询前的 PowerShell 查找和 Electron / Node 包装运行方式；本机实际安装解析到平台依赖内的 codex.exe。
- 25 项单元测试通过，包括 npm 原生路径、独立 EXE、空格路径、ARM64 vendor 布局、缺失原生包拒绝回退及查询隐藏参数。
- 打包测试采用模拟 npm 目录中的原生控制台测试程序，通过 Windows GetConsoleWindow / IsWindowVisible 检查控制台不可见；测试没有调用真实额度服务。
- 默认启动、日志事件、本地手动刷新与 10 秒定时补查均不调用官方服务；原生联动测试断言查询次数保持不变。打包测试先验证没有 CLI 调用，再主动点用官方查询路径验证隐藏控制台。

## 0.3.6 添加账号浏览器修复（2026-09-15）

- 原实现收到 CLI 授权链接后仅更新状态，首次打开浏览器依赖 CLI；管理工具没有主动调用桌面浏览器。新增两项回归测试在旧实现上超时失败。
- 收到完整的官方授权链接后，通过 Electron shell.openExternal 主动打开。stdout / stderr 分别按完整行解析，避免分段输出截断链接；管理工具不会因重复输出再次打开同一会话。
- 系统打开失败时保留会话和链接，显示默认浏览器检查提示；“重新打开浏览器”复用同一流程，取消或结束后禁用按钮。未把原始 CLI 输出或系统异常展示给用户。
- 27 项单元测试与语法检查通过。scripts/login-browser-smoke.js 在源码及 --packaged 模式下验证真实 main / preload / renderer：点击添加、登录 IPC、调用浏览器、失败提示、重试、取消后禁用与临时目录清理。
- 登录联动检查替换了 CLI 和系统浏览器边界，未向真实授权服务提交测试数据，也未验证用户默认浏览器的实际授权结果。首次打包检查发现测试未等待账号界面初始化，增加就绪等待后通过。
- 0.3.6 本地安装包构建完成；scripts/packaged-smoke.js 验证实际 EXE 启动、临时账户库、IPC / DPAPI、悬浮窗与隐藏查询通过，当前真实 Codex 账号未改动。

## 0.3.7 Windows 保留回调端口修复（2026-09-15）

- 用户确认 0.3.6 仍打不开浏览器。实际运行本机官方 CLI 0.154.0，登录在生成链接前报套接字错误 10013；系统保留端口范围包含 1420–1519，覆盖 OAuth 回调端口 1455。没有修改系统保留端口或关闭其他程序。
- 同一 CLI 使用 --device-auth 成功返回 https://auth.openai.com/codex/device 和一次性验证码。新增检测回调绑定错误后自动切换设备码登录，界面展示验证码及输入说明。
- 新增回归测试先在旧实现失败，修复后 28 项测试通过。源码登录联动检查验证回调失败、第二个设备码子进程、浏览器调用、验证码显示和取消后隐藏。
- scripts/live-login-browser-check.js 使用隔离目录、真实 CLI 和真实 Electron shell.openExternal；验证获取设备码并打开官方页面后取消测试。用户明确确认“已经打开官方页面”。未完成实际账号授权保存，未更改当前 Codex 账号。
- 打包目录登录联动检查及实际 0.3.7 EXE 隔离验收通过；49 个打包源码文件与待提交源码逐字节一致。


## 0.3.8 托盘驻留切换修复（2026-09-19）

- 用户确认关闭主窗口后 Codex 仍在右下角托盘；原实现只发送 CloseMainWindow，15 秒后仍有进程即中止。
- 主界面与悬浮窗新增托盘退出重试，以及二次确认的「结束后台并切换」。默认与非布尔 force 参数均不能触发强制退出。
- `npm run check` 与 31 项测试通过；新增 PowerShell 进程夹具验证仅结束安装目录内的目标，不结束其他目录中的同名进程。
- Electron 隔离验收通过：实际主进程、IPC、DPAPI 和两个渲染界面，替换 OS 适配器并使用临时合成凭据，验证托盘错误恢复、取消不结束进程、显式确认后切换、退出失败不写凭据以及启动失败回滚。
- 未结束本次会话使用的真实 Codex，未执行真实账号切换；真实在线身份仍需用户切换后核对。
- v0.3.8 实际打包 EXE 的隔离启动、IPC、DPAPI、悬浮窗及解包后的 Windows helper 检查通过；49 个打包源码文件与当前源码逐字节一致。

## 0.3.9 主窗口关闭行为（2026-09-19）

- 原因：原 close 处理在悬浮窗不显示时调用 app.quit，导致关闭主窗口直接退出。修复为非退出流程始终 hide。
- `scripts/window-lifecycle-smoke.js` 在真实 Electron 和空的临时账户库中复现旧版失败；修复后验证悬浮窗不存在、可见、隐藏三种状态下关闭主窗口均不退出，主窗口可恢复，显式 app.quit 仍正常退出。
- 验证仅操作隔离的管理工具实例，不读取或修改真实账号，不结束 Codex。
- 31 项单元测试、完整合成账号切换回归通过；修正回归脚本在悬浮窗首次加载尚未结束时过早操作的竞争条件。
- `scripts/window-lifecycle-smoke.js --packaged` 对 app.asar 内实际主进程代码执行同一原生窗口 close/恢复/退出检查并通过；49 个打包源码文件与源文件一致。
- CDP 中调用 renderer 的 window.close 后求值出现超时，因此窗口行为改由原生 BrowserWindow.close 验证，避免把调试协议超时当作按钮行为结果。

## 0.4.0 macOS 适配（2026-09-19）

- 增加 macOS 原生应用发现、按 bundle 路径匹配进程、NSRunningApplication 正常退出、确认退出后写入凭据、open 重启以及显式强制退出。
- macOS CLI 解析不执行 npm JS 包装器；官方登录取消和额度子进程清理不再调用 Windows taskkill。
- 平台相关文案、系统菜单、标题栏和凭据保护标识适配；Windows 原有退出和 DPAPI 分支保留。
- Windows 本地 38 项测试通过，1 项 macOS 原生夹具按平台跳过。macOS CI 启用 arm64 / x64 测试和 DMG / ZIP 构建；原生测试使用临时合成 .app，不操作真实 Codex 或用户凭据。
- 尚未完成真实 Mac 用户登录与在线身份核对；安装包未做 Developer ID 签名 / 公证。

## 0.4.1 周额度异常归零保护（2026-09-21）

- 旧实现按事件时间直接接受本地 0 值；使用相同恢复时间的合成事件复现剩余额度从 59% 跳到 100%，修复后保持 59%。本次读取的真实账户快照未处于异常状态，未声称捕获用户原始故障事件。
- 显示合并、本地日志合并、已保存账号快照及实时返回值统一保留同周期最后有效额度；不完整窗口不能覆盖已知使用率。新恢复周期的 0 值和更新的官方查询仍可正常生效。
- Windows 单元测试 40 项通过、1 项 macOS 原生测试按平台跳过；JavaScript 语法检查通过。Electron 隔离测试覆盖主窗口、悬浮窗、文件监听和定时刷新，异常 0 值不再反弹成 100%。
- 自动同步保持本地读取，验证没有增加远程请求且合成凭据未变化。未操作真实 Codex 进程或改写真实凭据。
- 补充验证：日志轮换为仅有异常 0 值时，已保存的账号额度仍能保护主界面和悬浮窗。v0.4.1 实际 EXE 的隔离启动、IPC、DPAPI、悬浮窗检查通过，打包源码逐字节核对通过。

## 0.5.0 开发中：蒸馏与记忆（2026-09-21）

- 独立实现，没有复制参考项目代码、提示词或界面素材。
- Windows 47 项单元检查通过，1 项 macOS 原生检查按平台跳过。新增检查覆盖选段边界、工具/系统正文排除、指纹变化拒绝、草稿来源、敏感字段遮盖、并发编辑、损坏数据保留、CLI UTF-8 分段输出、隐藏启动、取消及错误输出隔离。
- 实际 Electron 主进程、preload 与页面集成检查通过：确认发送前未调用模型，未选中内容未进入模型输入，草稿转记忆、系统加密、明文导出、重载恢复、取消不新增条目、蒸馏时切换受阻。只使用隔离目录与合成凭据，没有操作真实 Codex 进程。
- 本机官方 CLI 0.154.0 使用当前账号对一段合成偏好文本完成真实联网调用，返回中文结果；这不等于已验证所有用户历史会话或所有模型。
- 本次尚未进行 macOS 新功能集成与安装包验证；v0.4.1 的 Mac 构建证据不能替代此版本验证。

- 新增超时后子进程不发送 close 事件的测试；仍返回明确失败，不无限占用任务状态。v0.5.0 打包 ASAR 的知识库端到端检查、深色窄窗口布局及实际 EXE 启动检查通过。

## 0.5.0 开发中：全局设计与性能（2026-09-21）

- 8 个主页面、深浅主题及 360 × 560 Pro / Plus 悬浮窗完成隔离截图检查；修复双额度布局底部溢出。
- 新增并发去重 / 缓存失效竞争、会话缓存 / 文件追加测试。Windows 49 项通过，1 项 macOS 原生检查跳过；语法检查通过。
- 交互回归验证：无变化刷新保留 DOM、操作结束按钮恢复、消息原位勾选、分页选择、素材变更取消旧选择、记忆检索与任务进度不打断输入。
- 实际 main / preload 的知识流程回归通过；额度实时回归验证两个窗口更新，并保留同周期 51% 剩余额度和日志轮换保护。一次同时运行多组窗口脚本时主窗口最终断言失败；单独复查后通过，并补充了每次新额度到达主窗口的 9 秒时限断言。补充并行复查：同时运行界面脚本与额度实时脚本均通过，未再次复现；未将这些结果视为所有时序无故障的证明。
- 性能采用合成数据与提交 `55e95f0` 对比，含首次解析较慢的结果，详见 [界面与性能验证](UI_PERFORMANCE.md)。
- Windows 本地 v0.5.0 安装包已重新生成；实际 EXE 隔离测试、打包 ASAR 的蒸馏 / 记忆流程、账号切换 / 回滚夹具通过。57 个源码与资源文件和 ASAR 逐字节一致。
- 此次本地安装包 SHA-256：`9008c49e814dda2284820aba8b357656aa9539a10d133849a6b22f0885474690`。尚未推送源码或发布新版 Release，也未生成新版 DMG。

## 0.5.0 开发中：本地模型与启用项目范围（2026-09-21）

- 从本机 Codex 模型缓存读取可见模型及其支持的推理强度；本机配置仅用于默认偏好。模型切换联动推理选项，实际执行前重新校验目录，选择通过独立 CLI 参数传入。
- 先读取只读项目 / 会话索引，再读取启用项目的未归档 Codex 会话正文。排除归档、移除项目、ChatGPT 与子代理会话；项目或归档状态不可确认时停止读取。缓存命中也重新检查归档状态。
- 列表显示项目名称；「全选」选择当前会话跨页的全部可选消息，再次点击取消全选。每个会话最多保留最近 500 条，单次发送 60,000 字符上限保留。
- Windows 56 项测试通过，1 项 macOS 原生测试跳过；语法与差异空白检查通过。新增检查覆盖项目启用状态、归档后的缓存阻断、模型目录过滤、推理强度校验和 CLI 参数传递。
- 打包 ASAR 的实际 main / preload 流程验证模型选择、发送确认、草稿、保存、导出、重载、取消及切换保护；模型返回使用合成夹具，本轮未发起真实联网蒸馏。补充截图非空校验并刷新文档配图。
- 实际 Windows EXE 隔离启动、IPC、DPAPI、悬浮窗检查通过；59 个源码与资源文件和 ASAR 逐字节一致。
- 最新本地安装包 SHA-256：`c53039c9539c3c4efe887a0abdd52966c40aa8337e2facc511f8118ec49689b0`。尚未推送 GitHub、发布 Release 或生成新版 DMG。

## 0.5.0 开发中：归档误判与失效素材清理（2026-09-21）

- 只读检查本机 54 条候选元数据，发现 40 条索引路径与真实文件路径字面不同、规范化后相同；未读取真实聊天正文。Windows 扩展路径合成测试在修复前复现截图中的归档错误，修复后通过。
- 文件先解析真实路径并检查目录范围，再比较文件身份；真实归档或移除项目仍拒绝读取。不可用会话清除缓存并返回可处理的状态，界面移除对应条目、正文和已选消息，不展示内部 IPC 错误。重新读取列表及发送确认前也清理失效选择。
- Windows 57 项测试通过，1 项 macOS 原生测试跳过；语法检查通过。实际 main / preload 与打包 ASAR 均验证扩展路径、先选消息后归档、自动移除与零错误提示；使用隔离合成数据，没有调用真实模型或更改真实凭据。
- 59 个打包源码与资源文件和本次源码逐字节一致。本次重新生成的本地 Windows 安装包 SHA-256：`50d756e03150556af88bf3bb92dfd2bd85fefcf09565b0a3f34417ac5b1bf974`。未发布 GitHub Release，未构建新版 DMG。

## v0.5.0 跨平台发布验收（2026-09-21）

- 构建源码提交：`1c32a441f96c4cf532522c7302ac72fd2612f5b2`。后续发布提交只更新文档与演示图片，不改变应用源码或依赖。
- [GitHub Actions 35611375983](https://github.com/SanjiFlip/codex-auth-manager/actions/runs/35611375983) 的 Windows、macOS arm64 与 x64 三项任务全部通过。Windows 57 项单元测试通过、1 项按平台跳过；两种 Mac 各 54 项通过、4 项按平台跳过。
- 首轮 Mac 界面检查发现 Plus 双额度悬浮窗溢出；修正行高与垂直间距后，两种 Mac 的 8 页面、深浅主题、长列表及 360 × 560 悬浮窗检查通过，字号保留。
- 两种 Mac 均完成 Keychain、原生窗口、隔离账号切换、蒸馏与记忆检查，生成 DMG / ZIP，并通过打包后窗口、知识流程与实际应用账号导入检查。真实浏览器登录和 Codex 在线身份不在本轮合成检查结论内。
- Windows 安装包重新构建并通过实际 EXE 及打包知识流程检查。Windows ASAR 与本机源码一致；下载的两种 Mac ZIP 内各 59 个源码与资源文件和构建提交逐字节一致。
- README 的 11 张图片均重新生成自 v0.5.0 隔离合成数据；版本、下载链接、更新日志、使用说明与安全维护版本同步更新。安装包仅放入 GitHub Releases，未加入 Git 源码。
- 本版仍是预览版：Windows 未签名；Mac 为 ad-hoc 签名，未做 Developer ID 签名或公证。

发布文件 SHA-256（与 Release 的 `SHA256SUMS-0.5.0.txt` 一致）：

```text
517293d8fa163a16896efa5858b515369041da4752e76a1e6b9f27819c6c8633  Codex-Auth-Manager-0.5.0-arm64.dmg
d9a1f91e146af31d29ed2b2c506cb99190721901079fb75d4344a60873850d2a  Codex-Auth-Manager-0.5.0-arm64.zip
5bcdc5460e3b63955e66d484d24652dff5f9166dd6070002cfec1818679c73bc  Codex-Auth-Manager-0.5.0-x64.dmg
afc2838f6298d12f4aa902ca00d20bccd9f46995ffa7b9d5a27618c6d28f720c  Codex-Auth-Manager-0.5.0-x64.zip
a6290b6e57f24673d8ef053b8492cdebf08287daaa47c1fe42617562f0b1333b  Codex-Auth-Manager-Setup-0.5.0-x64.exe
```

## 项目分组会话导航（本地未发布，2026-09-22）

- 将平铺会话卡片改为项目折叠列表，按项目 ID 分组，默认折叠。展开先显示 5 条会话标题，再按需增加；移除重复的项目、日期和条目数量信息，右侧显示当前会话名称和消息。
- 原生按钮提供键盘操作、展开状态与长标题提示。搜索自动展开匹配项目，折叠保留已选消息；项目展开本身不读取会话正文。
- 界面检查覆盖 150 个合成会话的分组、搜索、展开 / 收起、展示更多、跨页全选、选择保留，以及 8 页面深浅主题和窄窗口布局。实际 main / preload 与打包 ASAR 的蒸馏、保存、导出、取消、归档后移除检查通过；没有读取真实会话正文或调用真实模型。
- 语法与差异空白检查通过。59 个打包源码与资源文件与本次源码逐字节一致；Windows 安装包已重新生成。界面截图保存在 `output/knowledge/project-sessions.png`。
- 此次本地 EXE 的 SHA-256：`293937636f71fb53cf03b7e0898025e4bad1ce73bbca85f84221726a3e7c33f2`。未提交 GitHub、更新 Release 或构建新 DMG，已发布的 v0.5.0 安装包仍为此前版本。


## 项目 / 会话勾选与两阶段蒸馏（本地未发布，2026-09-22）

- 新增会话与项目复选框，半选同步到具体消息。150 会话合成界面覆盖折叠、筛选外整项目选择、取消、归档变更失败时保留原选择；切换与展开不会自动启动蒸馏。
- 新增 5 组分批、结构化证据与整合测试：完整消息与来源编号保持、字符与批次限制、虚构原文/跨批来源拒绝、偏好必须含用户原文、非法整合引用拒绝、取消停止后续调用、12 会话预览不调用模型、变更指纹/无效输出不保存。
- `npm test`：63 项，62 通过、1 项原生 macOS 检查在 Windows 跳过。`npm run check`、`git diff --check` 通过。
- `scripts/design-smoke.js`：8 页面深浅主题与窄窗口、Pro/Plus 悬浮窗、项目批量勾选检查通过。
- `scripts/knowledge-smoke.js` 源码及 `--packaged`：实际 main / preload / DPAPI 下的项目与会话勾选、半选、调用次数预览、两阶段模型替身、草稿保存/导出/重载、取消、切换账号保护与归档移除通过。
- `scripts/packaged-smoke.js`：实际 Windows EXE 启动、隔离库、原生悬浮窗、IPC 与凭据保护检查通过。62 个源码、资源和许可证文件与 ASAR 逐字节一致；构建器整理后的 package.json 已核对运行字段。
- `npm run dist` 已完成；本地 EXE SHA-256：`71249703bfbcfdd9486fbb56fc7d1d59adc7f2abeb6a982148e9bc5bf351b2d6`。覆盖本地开发安装包，不替换 `release/publish-v0.5.0` 或 GitHub 已发布文件。
- 本机 CLI `exec --help` 已确认支持 `--output-schema`，替身进程检查验证 Schema 文件存在、参数传递和临时目录清理。本次未调用真实在线模型、读取真实聊天、安装替换运行中的工具或构建新 DMG；合成测试不证明模型输出在真实项目中的事实准确性。
- 最新勾选界面使用合成内容截图，见 `docs/images/distillation-selection.png`；实现、技术参考与维护边界见 `docs/KNOWLEDGE.md`。


## Skills 管理与会话发现（本地未发布，2026-09-22）

- 会话漏项先由 4 个回归测试复现失败，再修复：超过 32 MB 的本地会话、失效 rollout 路径、缺正文后出现、305 个合格会话及索引活动时间。修复后 4 项通过，归档 / 启用项目门禁保留。
- 本机仅元数据核对：54 个合格会话中有 10 个超过旧 32 MB 门槛，另有 5 个修改时间超过 30 天。修复后列表包含 54 / 54，未读取真实聊天正文。用户描述的“点击后才出现”的完整前后状态无法追溯，本次结论限于已确认的漏项原因和回归场景。
- `npm test`：72 项，71 通过、1 项原生 macOS 检查在 Windows 跳过。`npm run check` 通过；目录提示调整后再次通过 5 项 Skills 测试。
- `scripts/skills-config-smoke.js`：本机官方 CLI、临时 CODEX_HOME 的真实配置接口验证启用 / 停用、保留无关设置和注释；未请求账号、额度或模型。
- Skills 仓库夹具测试覆盖 YAML、路径、缓存离线、固定提交下载、blob 完整性、符号链接拒绝、无覆盖、已有 Skills Hub 链接合并、场景组合和版本冲突。
- 公开仓库下载补充检查：在系统临时目录读取 OpenAI 技能目录（39 项），固定提交 `49f948faa9258a0c61caceaf225e179651397431` 安装 `gh-fix-ci` 的 6 个文件，逐文件摘要核对通过。未安装到用户技能库，未执行技能脚本，检查后清理临时目录。
- `scripts/skills-smoke.js` 源码及 `--packaged` 均通过：实际 main / preload / 页面，合成商店、安装预览、许可证、安装不自动启用、多组组合 / 场景切换、保留现有链接、编辑、重载及窄窗口。配置写入使用替身；真实官方配置接口由上一项独立验证。
- `scripts/design-smoke.js`：9 页深浅主题、窄窗口、悬浮窗、项目勾选与 145 项技能库渐进展示通过。`scripts/knowledge-smoke.js` 源码及打包检查通过；模型返回使用合成夹具，未验证真实蒸馏内容质量。
- `npm run dist` 完成。实际 Windows EXE 启动 / 隔离库 / 原生悬浮窗 / 凭据保护检查通过。68 个源码、资源和许可证文件与 ASAR 逐字节一致，package.json 运行字段一致。
- 最新本地 EXE SHA-256：`4b4a5aa7741ed8daf6861e0cf396bd1955c1d59a86baa1a9cdf9599102887bbd`。此前同名本地开发安装包已更新；`release/publish-v0.5.0` 和 GitHub 发布文件保持原版本。
- 新增两张隔离合成 UI 截图并更新 README / Skills 文档；Mac CI 增加 Skills 源码及打包检查，但本轮尚未在 Mac 运行或构建新 DMG。未提交或推送 GitHub，未替换用户当前运行的已安装程序，未改用户 Skills 配置。


## 大项目恢复与技能商店限流（本地未发布，2026-09-22）

- 大项目回归：104 万字符、65 批合成素材，780 条提取记录（中间证据大于旧 100000 字符限制），至少两层整合；每次输入小于 30000 字符、最终正文小于 80000 字符，完整证据保留。
- 恢复测试覆盖逐批持久化、暂停、重新创建实例、素材变化 / 归档拒绝、模型参数固定、整合遗漏来源关联拒绝、损坏检查点保留、草稿提交后中断的幂等恢复。
- `node scripts/resume-smoke.js` 及 `--packaged` 均通过：两次独立 Electron 应用进程共用隔离加密库。第一次第三批模拟失败；第二次启动显示恢复卡片，手动继续后前两批未重复调用，最终只有一份草稿，并完整导出证据及章节关联。模型返回均为夹具。
- GitHub 限流实际复现：公开 API 返回 403，`x-ratelimit-remaining=0`；原始文件正常。新增正确入口回归测试在修复前报同样的“GitHub 请求受限”，修复后通过。
- 真实网络验证：API 403 时，GitHub Git refs 和 codeload 均为 200；从固定提交 `49f948faa9258a0c61caceaf225e179651397431` 读取 39 个技能，在临时目录安装 `gh-fix-ci` 的 6 个文件并校验摘要。无 GitHub token、未修改用户技能库。
- Skills 新增限流首次加载、并发请求合并、跨实例冷却时间、缓存回退、标签解析和提交不匹配拒绝检查。`scripts/skills-smoke.js --packaged --limited` 验证实际 main / preload / UI 与备用压缩包解析依赖可用，场景安装和切换流程通过。
- `npm test`：81 项，80 通过、1 项 macOS 专用测试在 Windows 跳过。语法检查及 9 页深浅主题、项目勾选、窄窗口布局检查通过。批量字符统计改为增量累计，避免每加入一会话就重算全部已选正文。
- `npm run dist` 完成。70 个源码、资源和许可证文件与 ASAR 逐字节一致，package.json 运行字段一致；本地安装包 SHA-256：`94525dbb5a6e8c8e90985f8a3bbb3bb67571c20dbd521bb49d73edec8c42616a`。
- README / 知识使用说明 / Skills 说明及恢复界面合成截图已更新。本轮不调用真实蒸馏模型，不声明摘要事实正确性已验收；不读取真实聊天正文，不改用户技能配置。未发布 GitHub、新 DMG 或替换正在运行的已安装程序。
- 最终安装包的 `scripts/packaged-smoke.js` 通过：实际 Windows EXE 启动、隔离库、原生悬浮窗、IPC / DPAPI、主题与凭据保护；原始合成 auth 文件保持一致。


## 自定义技能来源、分组批量选择与额度回跳（本地未发布，2026-09-22）

- 自定义来源覆盖保存 / 编辑 / 移除、重启持久化、分支大小写、重复来源、旧库迁移、版本冲突和内置来源保护。移除来源后已安装文件和分组保持不变。
- 分组弹窗增加全选 / 取消全选、已选计数和禁用状态；隔离 UI 检查覆盖手动勾选联动、取消不保存与全选保存。
- `node --test tests/quota-refresh-regression.test.js` 在修复前复现 3 项失败：刷新后剩余 59% 被一秒后的本地零值改成 100%，本地合并也接受未到期的移动重置时间，模型专属额度覆盖账号总额度。修复后 3 项通过；原有周期重置测试调整为在旧周期到期后观察到新周期。
- 修复不以本机当前时间推断历史事件重置，而以事件观察时间核对旧周期边界；缺少边界时保留原测量。有效官方归零、真实周期重置以及同类别非零更新继续生效。尚未取得反馈者原始数据，结论限于已复现路径。
- `npm test`：86 项，85 通过、1 项 macOS 专用测试在 Windows 跳过。`npm run check`、`node scripts/validate-local-data.js` 通过。
- 更新 README / Skills 使用说明、更新日志和合成界面截图。未读取真实聊天或改动用户技能配置；本轮未提交推送或发布新 DMG。
- `scripts/skills-smoke.js --packaged --limited` 通过：真实 main / preload / ASAR，合成仓库及限流备用通道；自定义来源修改、重载后保留和移除，分组全选 / 取消全选 / 取消不保存检查通过。
- `scripts/packaged-smoke.js` 通过：实际 Windows EXE 的隔离账号先由官方 CLI 替身返回已用 41%，随后向本地日志写入重置时间偏移一秒的零值。确认真实读取器已将该记录存入隔离账户库，并在 12 次逐秒检查、文件监听及定时刷新后，主界面 / 悬浮窗仍显示剩余 59%。启动和后台刷新未调用额度 CLI，手动查询子进程无可见控制台；真实用户数据未参与。
- `npm run dist` 完成，70 个源码、资源与许可证文件和 ASAR 逐字节一致，package.json 运行字段一致。本地 EXE SHA-256：`10ef581e3507f7d722f7bbc44b460b86cdf460353b0dea55fc3b9b4960bf9382`。未修改 `release/publish-v0.5.0` 或替换正在运行的已安装应用。


## v0.6.0 发布检查（2026-09-22）

- 版本、界面页脚、README 下载名称、更新日志、安全说明和开发检查文档统一为 v0.6.0；保留上游许可及独立实现说明。
- Token 回归覆盖加密落盘、重新实例化读取、替换 / 删除、损坏文件、加密不可用、匿名冷却隔离、请求域名与重定向限制、无效 Token 和私有仓库拒绝。未使用真实 GitHub Token 测试应用功能。
- Windows 源码的设计检查（9 页、深浅主题及窄窗口）、跨窗口实时额度、知识工作流、两进程断点恢复及 Skills 含 Token 的真实 IPC / DPAPI 检查通过，输入均为隔离合成数据。
- [GitHub Actions 35684649381](https://github.com/SanjiFlip/codex-auth-manager/actions/runs/35684649381) 已完成且全部成功：Windows check、macOS 14 arm64、macOS 15 Intel x64。产物对应源码提交 `fbbdedba5c53bc63d0bbab77191039bbce075adc`。
- 两种 Mac 均通过原生窗口、Keychain、知识 / Skills 含 Token / 断点恢复 / 设计检查、DMG 与 ZIP 构建和实际打包应用检查；没有使用真实账号或模型请求。
- Windows `npm test` 为 89 项：88 通过、1 项 Mac 专用检查跳过。最终 EXE 的 Skills Token、额度默认零值回跳及两进程断点继续验证通过；71 个源码 / 资源 / 许可证文件与 ASAR 一致。
- 本地发布 EXE SHA-256：`1387de183e77cacc3c718263ba33a9cf06762451e8349b6b66d8111113f92aad`。全部平台发布文件、来源构建和摘要见 [v0.6.0 Release](https://github.com/SanjiFlip/codex-auth-manager/releases/tag/v0.6.0) 的校验文件。

- 文档提交后的一次 Intel CI 在商店 UI 检查中超时。用 500 毫秒首请求延迟稳定复现，确认是测试固定等待 100 毫秒后点击仍禁用的按钮；改为等待目录加载完成。相同延迟下源码和打包 Skills 流程均通过。此修复只改测试脚本，不改发布应用源码或安装包。

- 后续 arm64 CI 在记忆搜索断言时遇到延迟防抖回调（仍有 24 张卡片，预期 1 张）。将渲染器 120 毫秒搜索定时器延迟到 500 毫秒可稳定复现；会话与记忆搜索断言改为等待目标 DOM 状态，保留延迟夹具后完整 9 页面设计检查通过。修改仅涉及测试。


## v0.6.1 · 大型仓库与来源删除修复（2026-09-22）

- 修复前，新增私有仓库 / 大目录测试出现 4 项失败，真实 Electron IPC 检查复现“目录加载期间无法打开移除来源”。修复后这些回归通过。
- Windows `npm run check` 通过；`npm test` 共 96 项，95 通过、1 项 Mac 专用检查跳过。覆盖嵌套 702 技能完整目录、递归树截断继续读取、过大快照切换轻量目录、Git 隔离和清理、私有 blob 预览 / 安装、认证隔离和权限撤销。
- Windows 源码 Skills（含匿名限流）、9 页面设计、知识工作流、两个进程断点恢复、隔离账号 IPC / DPAPI 和窗口关闭恢复均通过。测试使用临时数据，不改真实账号或技能库。
- 公开科研工具箱实测：固定提交 `49c6e97775eaa18ba791bebe23162a70ae601c18`，API 返回 166 个技能约 1.6 秒，Git 轻量通道返回 2484 个文件条目、166 个技能约 2.6 秒。已预览 scientific-writing（31 个文件）；这些是本次环境的单次测量，不是跨网络性能保证。
- 用户指定私有仓库通过已有 GitHub CLI 的隔离认证传输验证：读取 31 个技能、预览并安装一个技能至临时目录后清理，私有目录未落盘缓存。未复制 gh 凭据至应用，未把私有名称或正文加入测试夹具；应用 Token / API 域名约束另由合成 Token 测试覆盖。
- Windows v0.6.1 EXE 构建成功；打包后的 Skills（含匿名限流、Token 加密、加载中删除）和实际 EXE 启动、主题 / Meter、额度防回跳检查通过。macOS 两架构构建 / 打包检查将在提交后的记录中补充；以上不代表真实用户登录切换或技能脚本运行验收。

- 可选翻译检查：101 项测试中 100 通过、1 项 Mac 专用跳过；覆盖服务轮换、缓存复用、失败切换和冷却、取消、代码 / URL 保留、UTF-8 分段和入参限制。真实 Electron IPC 验证默认无外发、开启后本地 / 市场 / 正文翻译、故障切换和偏好重载，测试响应均为合成内容。
- 两个公共接口均以通用测试短句实测返回中文；未发送任何真实本地或私有技能正文。当前网络连通不等同于所有中国运营商可用；不使用失效的 Edge 公共端点，也不绕过 Bing 验证码。
- 安装性能合成对比：32 个文件、每次下载人为延迟 25 毫秒，旧版串行 1002 ms，新版最多 4 并发 253 ms；仅证明该夹具下的并发收益，不代表真实网络提速比例。

- 打包后的翻译 IPC、默认关闭、手动开启 / 关闭、备用服务接管、本地 / 市场 / 正文译文与原文保留均通过；主界面 9 页和悬浮窗设计回归通过。新增翻译后的 macOS 构建结果将在发布前补充。

### v0.6.1 最终构建与发布核对

- [GitHub Actions 35688504382](https://github.com/SanjiFlip/codex-auth-manager/actions/runs/35688504382) 的 Windows check、macOS 14 arm64、macOS 15 Intel x64 全部成功，构建提交为 `6c3005cbfccef4e927da1c82359ab686c094509d`，标签为 `v0.6.1`。
- 两种 Mac 均完成源码测试、窗口 / 账号 / 知识 / Skills / 断点恢复 / 设计检查、DMG 与 ZIP 构建，以及打包应用检查。账户与模型请求使用隔离夹具，不据此声明真实账户切换或模型输出质量已验收。
- Windows 和两种 Mac 的最终 ASAR 各 74 个源码、资源与许可证文件逐一匹配构建源码；package.json 的版本、入口和运行依赖一致。Mac 比较基准为 Git 提交中的 LF 文件，Windows 为本地构建文件。
- Windows EXE SHA-256：`c5e3650310c36ac139c11e671513c3c864f78bf079f6aea663fedf31372bd154`。5 个安装 / 压缩文件的完整摘要收录于 `SHA256SUMS-0.6.1.txt`，与安装包一起提供；源码仓库不包含安装包和用户凭据。

## v0.6.2 本地验收（2026-09-22）

- 补齐本项目更新检查和按平台打开下载；设置页与侧栏版本由运行时读取。新增近 7 天用量 CSV 导出，修复商店已入库技能、删除所选分组和蒸馏预览确认与实际请求不一致的问题。
- `npm run check` 通过，`npm test` 共 126 项：125 通过，1 项原生 Mac 检查在 Windows 跳过。新增更新检查、CSV 转义、统计可用性与配置扫描回归。
- 真实 Electron main / preload / 页面检查验证：启动不查询更新、手动查询后明确点击才打开平台链接、缓存复用、不可信窗口拒绝、导出取消不写文件、原始 total-only 日志导出未知子项留空。更新请求使用合成传输；另一次真实公开 GitHub 查询正确识别已发布的 v0.6.1 预览版，不提示本地开发版降级。
- Skills 相关 28 项测试和隔离 UI 检查通过；已安装来源匹配包含旧 ID 与仓库大小写变化，未知来源或不同仓库的同名技能不误判。每次扫描 100 技能 / 100 条配置的 realpath 从 10,000 次降至 100 次，链接目标在下轮扫描重新解析。
- 合成 3000 会话 / 100 项目 / 30 会话批次的勾选检查：项目属性读取 18,606,000→18,000，选择 DOM 查询 93→6；同机单次 UI 测量 1143.6→7.5 ms，不包含真实磁盘 / IPC 成本。归档拒绝、取消保留原选择、预览单飞、慢预览期间修改配置及失败重试均覆盖。
- 九页面明暗主题 / 窄窗口 / 悬浮窗与知识工作流检查通过，README 用量和设置图片更新为合成数据。未读取真实会话或切换真实账号，不运行真实蒸馏模型。
- 独立审查发现一项 P2：导出接收到已经补零的统计结果，无法兑现未知子项留空。已增加解析与聚合层字段可用性元数据，以及临时原始日志→解析→聚合→CSV 的链路检查；保留原有 tokenUsage 数值算法、额度计算和事件键。
- 安装包最终核对与 macOS CI 结果在本节后续发布记录中补充；以上不把尚未完成的构建称为已发布。
- P2 复核通过：12 项定向测试全部成功，44 条合成事件在正常 / 分叉会话、3 个聚合窗口、2 个额度窗口中与原基线差分一致（忽略新增元数据及检查时间）；统计与额度数值未改变。独立审查未发现其他重要问题。
- 首轮 Windows ASAR 中 75 个源码 / 资源 / 许可证文件与当时工作区源码一致，package.json 运行字段一致。实际 EXE 启动、隔离凭据、原生 Meter、额度回跳回归及打包后的 Skills / 更新检查 / CSV 流程通过。
- 首轮 Windows EXE SHA-256：`e3ea93e3d03987772b5a1fd51732e9eb2eba053c07e9fabe1cacb52027314fb9`。这是翻译竞态修复前的历史构建，未作为本版发布资产；最终摘要见下方发布核对。
- 首轮 CI `35695931103`：Windows 原有 PowerShell 测试在 25 秒超时后失败；原提交不改逻辑、不加超时重新执行后通过，后续仅补充子进程错误诊断。Intel Mac 则暴露实际产品竞态：旧详情 close 回调误取消新开始的列表翻译。
- 对翻译竞态以受控顺序复现：挂起列表首请求，再释放旧详情的 close 事件。修复前稳定失败，新增详情任务归属后同用例通过；关闭当前详情仍会取消自己的任务，未扩大超时或加入重试。独立复核通过；最新 Windows 单元测试 128 项、127 通过、1 项 Mac 专用跳过。
- 下一轮 CI 的 Windows 同一原生测试再次触发 `spawnSync powershell.exe ETIMEDOUT`。改为独立测量旧 PowerShell 宿主 / 内置命令初始化（最多 60 秒），收到就绪行后才发送隔离 fixture，脚本操作仍限制 25 秒；退出码、目标终止和无关进程保护断言保持不变。仅调整测试宿主，不改生产退出流程；本地启动 187 ms、操作 81 ms，云端阶段结果待确认。

### v0.6.2 最终构建与发布核对

- [GitHub Actions 35697138884](https://github.com/SanjiFlip/codex-auth-manager/actions/runs/35697138884) 的 Windows check、macOS 14 arm64、macOS 15 Intel x64 全部成功。构建源码与标签 `v0.6.2` 指向 `47072fbae7b464bc6a8636e227be862df067a554`。
- Windows 128 项测试中 127 通过、1 项 Mac 专用跳过；两种 Mac 各 124 通过、4 项按平台跳过，均无失败。Windows 云端本次记录初始化 7157 ms、实际 fixture 1260 ms；该次分阶段测量通过，不能据此追溯认定前两次超时的具体阶段。
- 两种 Mac 均通过源码与打包后的窗口、知识工作流、断点恢复、Skills、更新检查及 CSV 检查，完成原生程序 / Keychain / Meter 检查与 DMG、ZIP 构建。数据、模型及认证均为隔离夹具。
- Windows 包已包含 `49ad3a6` 翻译竞态修复，重新构建后的 Skills 受控竞态与限流检查、更新检查 / CSV 导出检查通过。此后 `47072fb` 仅修改测试和验证文档，未改应用代码或资源。
- Windows 和两种 Mac 的最终 ASAR 各 75 个源码、资源与许可证文件匹配构建源码，package.json 的版本、入口及运行依赖一致。Mac 逐字节比较使用构建 Git 提交，Windows 使用本地构建文件。
- 最终 Windows EXE SHA-256：`dd929097de1685e953ffc60abfa17bfd3d4ead01b6c92f486343e35b3be9326c`。5 个发布安装 / 压缩文件的摘要收录在 `SHA256SUMS-0.6.2.txt`；前述 `e3ea93e3…` 历史构建不用于发布。
- 未替换用户正在运行的应用、改动真实账号或技能配置；上述验证不包含真实账号登录切换，也不证明真实蒸馏结果的事实准确性。
- [v0.6.2 Release](https://github.com/SanjiFlip/codex-auth-manager/releases/tag/v0.6.2) 已公开发布为预览版。6 个资产（EXE、两种架构的 DMG / ZIP、校验文件）的 GitHub 摘要及字节数与本地逐一一致，匿名下载 HEAD 均返回 200。
- 发布后更新查询的真实匿名 API 请求遇到 GitHub 限流，应用正确返回稍后重试提示，本次不宣称该网络路径成功。通过 GitHub CLI 读取实际发布元数据后，验证旧版 0.6.1 对 0.6.2 的版本比较和 Windows / Apple Silicon / Intel 下载映射均正确；该检查不复用 CLI 凭据到应用。用户遇到限流时可直接访问 Release 页面下载。
